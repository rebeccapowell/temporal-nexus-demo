# Fix Wave 2 — Critical/High Review Findings

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans

**Goal:** Fix 5 critical/high review findings on `feat/temporal-nexus-demo-initial-implementation`.

**Architecture:** Minimal surgical edits across 5 files + new shadcn/ui component layer. No schema changes; all workflow fixes preserve determinism.

**Tech Stack:** .NET 10, Temporal .NET SDK 1.15.0, React 18 + Vite + Tailwind v4 + shadcn/ui primitives

## Global Constraints

- Runtime: .NET 10
- Temporal: `Temporalio` v1.15.0 — signal/query names: SDK strips "Async" suffix (e.g., `EmailVerifiedAsync` → `"EmailVerified"`)
- Frontend: React + Vite + Tailwind v4 + shadcn/ui + pnpm
- No application database; all state in Temporal workflows
- Keep patches minimal — no unrequested refactors

---

### Task 1 — Late email-verification must unblock active CheckoutWorkflow

**Files:**
- Modify: `src/Checkout.Worker/Activities/CheckoutActivities.cs`

After `NotifyEmailVerifiedAsync` signals `PurchaseIntentWorkflow`, query PI state and — if a checkout is active — also signal `CheckoutWorkflow.EmailVerifiedAsync` (signal name `"EmailVerified"`).

- [ ] Edit `NotifyEmailVerifiedAsync`: add `using ShoppingBasket.Contracts;` then query PI, forward signal to checkout handle
- [ ] Build `Checkout.Worker` — 0 errors
- [ ] Commit

---

### Task 2 — Token must be validated before marking email verified

**Files:**
- Modify: `src/Checkout.Worker/Workflows/EmailVerificationWorkflow.workflow.cs`

Store expected token, compare in `VerifyAsync`.

- [ ] Add `private string? _expectedToken;` field
- [ ] In `RunAsync`, set `_expectedToken = input.VerificationToken;`
- [ ] In `VerifyAsync(string token)`, only set `_verified = true` if `token == _expectedToken`
- [ ] Build `Checkout.Worker` — 0 errors
- [ ] Commit

---

### Task 3 — Recovery email requires verified-email + inactivity (not just any email)

**Files:**
- Modify: `src/PurchaseIntent.Worker/Workflows/PurchaseIntentWorkflow.workflow.cs`

Gate the Phase 1 recovery send on `_status == Active` (not checkout-started/processing).

- [ ] Change condition from `!phase1Done && _email is { }` to `!phase1Done && _email is { } && _status == PurchaseIntentStatus.Active`
- [ ] Build `PurchaseIntent.Worker` — 0 errors
- [ ] Commit

---

### Task 4 — Checkout start must be idempotent / prevent duplicate active checkouts

**Files:**
- Modify: `src/PurchaseIntent.Worker/Workflows/PurchaseIntentWorkflow.workflow.cs`
- Modify: `src/Storefront.Api/Endpoints/PurchaseIntentEndpoints.cs`

Server-side guard in workflow update validator + API pre-check with 409 Conflict.

- [ ] Add `[WorkflowUpdateValidator(nameof(StartCheckoutAsync))]` method to PurchaseIntentWorkflow
- [ ] API: if `piState.CheckoutId != null && piState.Status == CheckoutStarted`, return 409
- [ ] Build solution — 0 errors
- [ ] Commit

---

### Task 5 — shadcn/ui component layer

**Files:**
- Create: `src/Storefront.Ui/src/lib/utils.ts`
- Create: `src/Storefront.Ui/src/components/ui/button.tsx`
- Create: `src/Storefront.Ui/src/components/ui/input.tsx`
- Create: `src/Storefront.Ui/src/components/ui/card.tsx`
- Modify: `src/Storefront.Ui/src/App.tsx` (import + use components)
- Modify: `src/Storefront.Ui/src/index.css` (CSS variables)

- [ ] `pnpm add class-variance-authority clsx tailwind-merge lucide-react`
- [ ] Create `src/lib/utils.ts` with `cn`
- [ ] Create 3 UI primitives (Button, Input, Card)
- [ ] Add CSS variables to `index.css`
- [ ] Update `App.tsx` to use components
- [ ] `pnpm build` — 0 errors
- [ ] Commit
